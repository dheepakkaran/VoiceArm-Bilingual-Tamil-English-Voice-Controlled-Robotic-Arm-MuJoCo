# docs/

Media the README embeds: the demo GIF and a couple of screenshots.

`media/demo.gif` comes from `scripts/make_gif.py`. GitHub shows a relative
`.mp4` as a link rather than a player, so a GIF is the only thing that animates
in a README.
