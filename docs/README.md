# docs/

`media/demo.gif` is the animation in the README, produced by
`scripts/make_gif.py`. GitHub shows a relative `.mp4` as a link rather than a
player, so a GIF is the only thing that animates there.
