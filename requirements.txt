# What the code imports. One list -- everything runs on PyTorch, including on
# a Mac, so there is no separate set for Colab.

mujoco==3.12.0
numpy>=2.0
scipy>=1.15

opencv-python>=4.10        # rendering the demo GIF
pillow>=11.0

torch>=2.6
torchvision>=0.21          # transformers' image pipeline for OWLv2
transformers>=4.45

sounddevice>=0.5           # microphone

pytest>=8.0
