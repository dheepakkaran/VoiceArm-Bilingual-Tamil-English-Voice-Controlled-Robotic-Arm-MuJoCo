# Dependencies for Colab, or any machine without MLX: PyTorch and transformers
# instead. src/backend.py picks between the two at import time.
#
# requirements.txt is the local Apple Silicon set. Kept separate rather than
# merged with extras because the two stacks share almost nothing above numpy.

# simulation
mujoco==3.12.0
numpy>=2.0
scipy

# vision and media -- headless opencv, since there is no display
opencv-python-headless
pillow

# models
torch
transformers>=4.45
accelerate                 # device_map="auto" in scripts/bench_planner.py

# episode log
pandas
pyarrow

# web app
gradio==6.26.0
